<section id="viewCatalog">
    <div class="posts">
        {{#if posts}}
            {{#each posts}}
                {{> post}}
            {{/each}}
        {{else}}
            <p>No posts in database.</p>
        {{/if}}
    </div>
</section>